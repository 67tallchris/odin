// write function that returns x in power n.
let x = prompt(input)

function pow() {
  // test if natural number, if not break out and give error message
  
  // take input equals x
  // multiply x by itself n times
}
